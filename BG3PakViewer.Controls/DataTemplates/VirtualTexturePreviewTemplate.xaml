<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
                    xmlns:vm="clr-namespace:BG3PakViewer.Controls.ViewModels"
                    xmlns:l="clr-namespace:BG3PakViewer.Locales;assembly=BG3PakViewer.Locales"
                    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
                    xmlns:b="clr-namespace:BG3PakViewer.Controls.Behaviors"
                    xmlns:c="clr-namespace:BG3PakViewer.Controls.Converters">
    <c:ImageToBitmapSourceConverter x:Key="ImageToBitmapSourceConverter" />
    <!--
      Preview for virtual textures: texture list on the left, and the
      selected texture decoded per-layer on the right (with layer selector).
    -->
    <DataTemplate x:Key="VirtualTexturePreviewDataTemplate" DataType="{x:Type vm:VirtualTexturePreviewViewModel}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition MinWidth="400" />
            </Grid.ColumnDefinitions>

            <ListView Grid.Column="0"
                      ItemsSource="{Binding Textures}"
                      SelectedItem="{Binding SelectedTexture}"
                      BorderBrush="Gray" BorderThickness="1" Padding="5"
                      VirtualizingStackPanel.IsVirtualizing="True"
                      VirtualizingStackPanel.VirtualizationMode="Recycling"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <i:Interaction.Behaviors>
                    <b:CollectionViewFilterBehavior Filter="{Binding TextureFilter}" />
                </i:Interaction.Behaviors>
                <!--
                  Every row must lay out at the same width, otherwise the copy button of a
                  long-named row drifts to the right and the buttons stop lining up.
                  Horizontal scrolling is therefore disabled (it measures rows at infinite
                  width), rows stretch to the full viewport, and long names are trimmed.
                -->
                <ListView.ItemContainerStyle>
                    <!-- ReSharper disable once Xaml.StaticResourceNotResolved -->
                    <Style TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:VirtualTextureItemViewModel}">
                        <Grid Margin="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Grid.Row="1" Grid.Column="0" Text="{Binding Dimensions}"
                                       Foreground="Gray" FontSize="11"
                                       TextTrimming="CharacterEllipsis" />

                            <!--
                              Copy-name button: uses the theme's default button chrome (no
                              custom template). The glyphs animate themselves through styles
                              bound to IsCopied, because VisualStateManager cannot resolve
                              Storyboard.TargetName across a DataTemplate's name scope.
                            -->
                            <Button Grid.Column="1" Grid.Row="0" Grid.RowSpan="2"
                                    Margin="4,0,0,0" Padding="0" Width="30" Height="30"
                                    HorizontalAlignment="Right" VerticalAlignment="Center"
                                    Command="{Binding CopyNameCommand}"
                                    AutomationProperties.Name="{I18N {x:Static l:LangKeys.CopyName}}"
                                    ToolTipService.ToolTip="{I18N {x:Static l:LangKeys.CopyName}}">
                                <Grid>
                                    <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Copy}"
                                                 RenderTransformOrigin="0.5 0.5">
                                        <FrameworkElement.RenderTransform>
                                            <ScaleTransform ScaleX="1" ScaleY="1" />
                                        </FrameworkElement.RenderTransform>
                                        <ui:FontIcon.Style>
                                            <Style TargetType="ui:FontIcon">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCopied}" Value="True">
                                                        <DataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Duration="00:00:00.15" To="0"
                                                                            Storyboard.TargetProperty="RenderTransform.ScaleX" />
                                                                    <DoubleAnimation Duration="00:00:00.15" To="0"
                                                                            Storyboard.TargetProperty="RenderTransform.ScaleY" />
                                                                    <DoubleAnimation BeginTime="00:00:00.1"
                                                                            Duration="00:00:00.01" To="0"
                                                                            Storyboard.TargetProperty="Opacity" />
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.EnterActions>
                                                        <DataTrigger.ExitActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation BeginTime="00:00:00.15"
                                                                            Duration="00:00:00.15" To="1"
                                                                            Storyboard.TargetProperty="RenderTransform.ScaleX" />
                                                                    <DoubleAnimation BeginTime="00:00:00.15"
                                                                            Duration="00:00:00.15" To="1"
                                                                            Storyboard.TargetProperty="RenderTransform.ScaleY" />
                                                                    <DoubleAnimation BeginTime="00:00:00.15"
                                                                            Duration="00:00:00.15" To="1"
                                                                            Storyboard.TargetProperty="Opacity" />
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.ExitActions>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ui:FontIcon.Style>
                                    </ui:FontIcon>

                                    <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.CheckMark}"
                                                 Opacity="0"
                                                 RenderTransformOrigin="0.5 0.5">
                                        <FrameworkElement.RenderTransform>
                                            <ScaleTransform ScaleX="0" ScaleY="0" />
                                        </FrameworkElement.RenderTransform>
                                        <ui:FontIcon.Style>
                                            <Style TargetType="ui:FontIcon">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsCopied}" Value="True">
                                                        <DataTrigger.EnterActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation BeginTime="00:00:00.15"
                                                                            Duration="00:00:00.15" To="1"
                                                                            Storyboard.TargetProperty="RenderTransform.ScaleX">
                                                                        <DoubleAnimation.EasingFunction>
                                                                            <BackEase EasingMode="EaseOut"
                                                                                    Amplitude="0.2" />
                                                                        </DoubleAnimation.EasingFunction>
                                                                    </DoubleAnimation>
                                                                    <DoubleAnimation BeginTime="00:00:00.15"
                                                                            Duration="00:00:00.15" To="1"
                                                                            Storyboard.TargetProperty="RenderTransform.ScaleY">
                                                                        <DoubleAnimation.EasingFunction>
                                                                            <BackEase EasingMode="EaseOut"
                                                                                    Amplitude="0.2" />
                                                                        </DoubleAnimation.EasingFunction>
                                                                    </DoubleAnimation>
                                                                    <DoubleAnimation BeginTime="00:00:00.15"
                                                                            Duration="00:00:00.015" To="1"
                                                                            Storyboard.TargetProperty="Opacity" />
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.EnterActions>
                                                        <DataTrigger.ExitActions>
                                                            <BeginStoryboard>
                                                                <Storyboard>
                                                                    <DoubleAnimation Duration="00:00:00.15" To="0"
                                                                            Storyboard.TargetProperty="RenderTransform.ScaleX" />
                                                                    <DoubleAnimation Duration="00:00:00.15" To="0"
                                                                            Storyboard.TargetProperty="RenderTransform.ScaleY" />
                                                                    <DoubleAnimation Duration="00:00:00.15" To="0"
                                                                            Storyboard.TargetProperty="Opacity" />
                                                                </Storyboard>
                                                            </BeginStoryboard>
                                                        </DataTrigger.ExitActions>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ui:FontIcon.Style>
                                    </ui:FontIcon>
                                </Grid>
                            </Button>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <GridSplitter Grid.Column="1" Width="10" HorizontalAlignment="Center"
                          VerticalAlignment="Stretch" Background="Transparent"
                          ResizeBehavior="PreviousAndNext" />

            <Border Grid.Column="2" BorderBrush="Gray" BorderThickness="1" Padding="10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="{I18N {x:Static l:LangKeys.GtsLayerLabel}}" VerticalAlignment="Center" />
                        <ComboBox Width="120" Margin="8,0,0,0" VerticalAlignment="Center"
                                  ItemsSource="{Binding Layers}"
                                  SelectedIndex="{Binding SelectedLayerIndex}" />
                        <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center"
                                   Margin="16,0,0,0" Foreground="Gray" />
                    </StackPanel>

                    <Border Grid.Row="1" Margin="0,0,0,8"
                            BorderBrush="Gray" BorderThickness="1">
                        <Viewbox Stretch="Uniform" StretchDirection="DownOnly">
                            <Image
                                Source="{Binding Preview, Converter={StaticResource ImageToBitmapSourceConverter}}"
                                RenderOptions.BitmapScalingMode="HighQuality" />
                        </Viewbox>
                    </Border>

                    <!-- ReSharper disable once Xaml.StaticResourceNotResolved -->
                    <ProgressBar Grid.Row="2" Height="4" Value="{Binding Progress}"
                                 Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </Grid>
            </Border>
        </Grid>
    </DataTemplate>
</ResourceDictionary>